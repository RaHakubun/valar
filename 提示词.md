    """任务类型表：
    第一行：音频，图像，视频，3D
    第二行：音频：语音生成
            图像：文生图，图生图，图像编辑
            视频：文生视频，图生视频，视频编辑
            3D：图生3D
    类型输入输出约束规则：
    语音生成：输入为文本，输出为语音
    文生图：输入为文本，输出为图像
    图生图：输入为图像，输出为图像
    图像编辑：输入为图像，输出为图像
    文生视频：输入为文本，输出为视频
    图生视频：输入为文本，输出为视频
    视频编辑：输入为视频，输出为视频
    3D：输入为图像，输出为3D模型

    工作步骤: 
    1.判断需求中每个子句的输入输出类型。遵守类型输入输出约束规则。
    2.根据用户需求，分析每个子句在第一行中需要包含的任务类型。
    3.根据第2步选出的任务类型，分析每个子句在第二行中需要包含的具体任务类型。
    输出：
    按顺序给出，输出第三个步骤的结果。
    example：
    输入：You are given an image `large_castle.png`, which is a photo of a large castle. First follow its style to generate a new image of a large church. Then convert it into a 2-second video of the church. Finally, upscale it by 2x and interpolate it to increase the frame rate by 3x. The result should be a high-quality video of a church.
    输出：
    图像编辑，图生视频，视频编辑，视频编辑

    输入：First generate an image of a woman riding a motorcycle on a highway. Then replace the background with a modern city skyline at night. The result should be a high-quality image of a motorcycle rider with a city skyline background.
    输出:文生图,图像编辑"""